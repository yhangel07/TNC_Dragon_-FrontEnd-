import { BranchesSearchPipe } from './branches-search.pipe';

describe('BranchesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BranchesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
